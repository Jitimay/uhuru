import 'package:flutter/material.dart';
class Entrepreneur extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Women')),
      body: const Center(child: Text('Women Category')),
    );
  }
}
